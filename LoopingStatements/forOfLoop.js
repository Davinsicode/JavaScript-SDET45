// By default it is taking the index values but we cant able to print the index value
let packages = ["friends", "family", "couple"]
for (index of packages) 
{
    // In ( For Of Loop ) we need to mention Index to print than it will print that particular indexed matching value
console.log(index);    
}